# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Feature 21 (canvas autosave) — Complete

## Current Goal

- Define the immediate implementation goal here.

## Completed

- **01-design-system**: shadcn/ui initialized (Nova preset, Tailwind v4), all UI primitives added (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea), lucide-react installed, `lib/utils.ts` with `cn()` created, `globals.css` updated with dark-only theme and project color tokens.
- **02-editor-chrome**: `components/editor/editor-navbar.tsx` — fixed-height top navbar (h-12, z-50) with left/center/right sections; sidebar toggle uses `PanelLeftOpen`/`PanelLeftClose` icons driven by `isSidebarOpen` prop. `components/editor/project-sidebar.tsx` — fixed overlay sidebar (w-72, z-40, top-12) that slides in from the left without pushing content; `isOpen`/`onClose` props; Projects header with close button; Tabs (My Projects, Shared) with empty placeholder states; full-width New Project button with Plus icon. Dialog pattern is satisfied by the existing shadcn Dialog component which already uses project color tokens via CSS variable mappings in `globals.css`.
- **03-auth**: Clerk wired into the app. `proxy.ts` at project root uses `clerkMiddleware` + `createRouteMatcher` to protect all routes except `/sign-in` and `/sign-up` (defined via `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` env vars). `ClerkProvider` wraps root layout with `dark` theme from `@clerk/ui/themes` and CSS variable overrides — no hardcoded colors. `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` use a two-panel layout (left: logo + tagline + feature list on lg+; right: Clerk form) with no gradients or hero sections. `app/page.tsx` redirects authenticated users to `/editor` and unauthenticated users to `/sign-in`. `app/editor/page.tsx` created as the editor workspace shell. `UserButton` added to the editor navbar right section.
- **04-project-dialogs**: `hooks/use-project-dialogs.ts` manages dialog kind (`create` | `rename` | `delete` | null), target project, form input, slug derivation, and mock project list (add/rename/delete in local state). `components/editor/project-dialogs.tsx` renders four controlled dialogs — Create (name input + live `ghostai.app/{slug}` URL preview, Enter submits), Rename (pre-filled input, `onFocus` selects all text for overtype, Enter submits, live URL preview), Delete step 1 (first confirmation: Cancel auto-focused so Enter never deletes, Delete project button advances to step 2), Delete step 2 (final confirmation: "Are you absolutely sure?", Cancel auto-focused, "Yes, delete forever" executes deletion; both close paths reset `deleteStep` to 1). All dialog titles use `text-lg font-semibold text-copy-primary`; inputs use explicit `text-copy-primary`. `components/editor/project-sidebar.tsx` updated with project list rendering: owned projects show rename/delete action buttons on hover; shared projects show no actions; mobile backdrop scrim (hidden on md+) closes sidebar on tap. `app/editor/page.tsx` updated with centered editor home (heading + description + New Project button) wired to Create dialog; all sidebar actions wired through the hook.
- **05-prisma**: `prisma/models/project.prisma` — `ProjectStatus` enum (`DRAFT`, `ARCHIVED`); `Project` model with ownerId (Clerk user), name, optional description, status, optional canvasJsonPath, timestamps, indexes on ownerId and createdAt; `ProjectCollaborator` model with composite PK `[projectId, email]` (enforces unique constraint), cascade-delete relation to Project, email, createdAt, indexes on email and `[projectId, createdAt]`. `lib/prisma.ts` — cached singleton using `globalThis`; branches on `DATABASE_URL`: `prisma+postgres://` → `new PrismaClient({ accelerateUrl })`, otherwise → `PrismaPg({ connectionString })` adapter. Migration `20260517075135_init` applied; client generated to `app/generated/prisma/`. Imports use `@/app/generated/prisma/client` (Prisma 7 custom output path).
- **06-project-apis**: `app/api/projects/route.ts` — `GET` returns all projects owned by the authenticated user ordered by `createdAt` desc; `POST` creates a project with name defaulting to `"Untitled Project"` if absent. `app/api/projects/[projectId]/route.ts` — `PATCH` renames project (requires non-empty `name` body field); `DELETE` removes project; both verify ownership and return `403` for non-owners. All four handlers return `401` for unauthenticated requests. Auth via `auth()` from `@clerk/nextjs/server`.
- **07-wire-editor-home**: `lib/projects.ts` — `getOwnedProjects()` fetches by `ownerId`; `getSharedProjects()` fetches via `ProjectCollaborator` by primary email using `currentUser()`. `POST /api/projects` updated to accept optional custom `id` (validated `[a-z0-9-]+`, max 100 chars). `hooks/use-project-actions.ts` — manages create/rename/delete dialog state; create slugifies name + generates 5-char random suffix as `roomId`, POSTs with custom `id`, navigates to `/editor/[id]`; rename PATCHes and refreshes; delete DELETEs and redirects to `/editor` if deleting active workspace or refreshes otherwise. `app/editor/page.tsx` — converted to server component; fetches owned and shared projects in parallel and passes to `EditorHome`. `components/editor/editor-home.tsx` — new client shell with sidebar state and dialog wiring. `components/editor/project-sidebar.tsx` and `project-dialogs.tsx` — updated to use `ProjectSummary` from `lib/projects`; sidebar receives separate `ownedProjects`/`sharedProjects` arrays; create dialog shows full `roomId` preview; rename dialog shows slug preview.
- **08-editor-workspace-shell**: `lib/project-access.ts` — `getCurrentIdentity()` returns `{ userId, email }` via `currentUser()`; `canAccessProject(projectId, userId, email)` checks owner match then falls back to `ProjectCollaborator` lookup. `components/editor/access-denied.tsx` — centered full-screen layout with Lock icon, short message, and link back to `/editor`. `app/editor/[roomId]/page.tsx` — server component; awaits params; redirects unauthenticated to `/sign-in`; shows `AccessDenied` for missing or unauthorized projects; passes project + owned/shared project lists to `WorkspaceShell`. `components/editor/workspace-shell.tsx` — client shell managing left sidebar and AI sidebar state; renders `EditorNavbar` with project name + share button + AI toggle, `ProjectSidebar` with `activeProjectId` highlight, canvas placeholder, and slide-over AI sidebar placeholder. `components/editor/editor-navbar.tsx` — extended with optional `projectName`, `onShare`, `isAISidebarOpen`, `onToggleAISidebar` props; renders project name in center and Share/Bot action buttons before `UserButton`. `components/editor/project-sidebar.tsx` — extended with optional `activeProjectId`; active project row renders with `bg-elevated` highlight.
- **09-share-dialog**: `lib/clerk-users.ts` — `enrichEmailsWithClerk(emails)` batches a `getUserList` call against Clerk Backend API and returns a `Map<email, { name, imageUrl }>`; falls back gracefully when no match. `app/api/projects/[projectId]/collaborators/route.ts` — `GET` lists collaborators (owner or collaborator access); enriches with Clerk data; returns `{ collaborators, isOwner }`. `POST` invites a new collaborator by email (owner only); validates email format; enforces unique constraint; returns enriched collaborator. `app/api/projects/[projectId]/collaborators/[email]/route.ts` — `DELETE` removes a collaborator by URL-decoded email (owner only). `components/editor/share-dialog.tsx` — client dialog; fetches collaborators on open; owners see invite form + remove buttons + copy link; collaborators see read-only list; Clerk avatar + display name shown when available, email-only fallback otherwise. `components/editor/workspace-shell.tsx` — added `isOwner` prop and `isShareOpen` state; mounts `ShareDialog`. `app/editor/[roomId]/page.tsx` — selects `ownerId` from project; passes `isOwner` to `WorkspaceShell`.
- **10-liveblocks-setup**: Installed `@liveblocks/node`, `@liveblocks/client`, `@liveblocks/react`. `liveblocks.config.ts` at project root — global `Liveblocks` interface augmentation defining `Presence` (`cursor: {x,y}|null`, `isThinking: boolean`) and `UserMeta` (`id`, `info.name`, `info.avatar`, `info.color`). `lib/liveblocks.ts` — cached `Liveblocks` node client singleton (dev-mode global caching pattern); `getUserColor(userId)` deterministically maps user ID to one of 14 cursor colors via hash. `app/api/liveblocks-auth/route.ts` — `POST` requires Clerk auth, parses `{ room }` from body, verifies project access via `canAccessProject`, calls `getOrCreateRoom` to ensure room exists with private default access, issues an access-token session with `userInfo` (name, avatar, color) and `FULL_ACCESS` for the room; returns `401` for unauthenticated, `400` for bad input, `403` for unauthorized project access.
- **11-base-canvas**: Installed `@liveblocks/react-flow` and `@xyflow/react`. `types/canvas.ts` — `NodeShape` union (rectangle, diamond, circle, pill, cylinder, hexagon); `NodeData` interface (label, color, shape); `CanvasNode` and `CanvasEdge` type aliases using `@xyflow/react` `Node`/`Edge` with `"canvasNode"`/`"canvasEdge"` type literals. `liveblocks.config.ts` — `Storage` typed as `{ flow: LiveblocksFlow<CanvasNode, CanvasEdge> }` using the `@liveblocks/react-flow` type. `components/editor/canvas-flow.tsx` — `"use client"` component; calls `useLiveblocksFlow` with `suspense: true` and empty initial nodes/edges; renders `ReactFlow` with loose connection mode, `fitView`, dot-pattern `Background`, `MiniMap`, and `Cursors`. `components/editor/canvas-wrapper.tsx` — `"use client"` wrapper; inline class-based `LiveblocksErrorBoundary`; mounts `LiveblocksProvider` (auth at `/api/liveblocks-auth`), `RoomProvider` (presence + `initialStorage` seeding the `flow` LiveObject), `ClientSideSuspense`, and `CanvasFlow`. `workspace-shell.tsx` — canvas placeholder replaced with `<CanvasWrapper roomId={projectId} />`.
- **12-shape-panel**: `types/canvas.ts` extended with `NODE_COLORS` (8 fill/text pairs matching ui-context), `NODE_SHAPES` array, `DEFAULT_NODE_COLOR`, `SHAPE_DEFAULTS` (per-shape default `width`/`height`), and `ShapeDragPayload` interface. `components/editor/canvas-node.tsx` — `"use client"` custom React Flow node; renders every shape as a bordered rectangle with label centered; 4 `Handle` components (top/right/bottom/left); background and text derived from `NODE_COLORS` lookup on `data.color`. `components/editor/shape-panel.tsx` — `"use client"` floating pill toolbar; 6 draggable buttons (Rectangle, Diamond, Circle, Pill, Cylinder, Hexagon) using Lucide icons; `onDragStart` serializes `ShapeDragPayload` as `application/json` into `dataTransfer`. `components/editor/canvas-flow.tsx` refactored: split into `CanvasFlow` (wraps in `ReactFlowProvider`) and `CanvasFlowInner` (uses `useReactFlow` for `screenToFlowPosition`); registers `canvasNode` in `nodeTypes`; `onDragOver` sets `dropEffect = "copy"`; `onDrop` reads payload, converts screen coords to flow position, builds a `CanvasNode`, and calls `onNodesChange([{ type: "add", item: newNode }])` to sync via `useLiveblocksFlow`; `ShapePanel` mounted in a `<Panel position="bottom-center">`.
- **13-node-shape**: `components/editor/canvas-shapes.tsx` — new shared module exporting `DiamondSVG`, `HexagonSVG`, `CylinderSVG` (each renders a 100×100 viewBox SVG scaled to node dimensions via `preserveAspectRatio="none"`), and `ShapeRenderer` (dispatches to CSS or SVG shape by type). `components/editor/canvas-node.tsx` — rewired to import SVG components from `canvas-shapes`; rectangle/pill/circle use CSS `rounded-xl`/`rounded-full` with `borderColor` inline style; diamond/hexagon/cylinder use absolute-positioned SVG fills; border color toggles between `#2a2a30` (rest) and `#00c8d4` (selected) via the `selected` prop from `NodeProps`. `components/editor/shape-panel.tsx` — added drag preview: `dragState` tracks active shape and cursor position; `onDragStart` calls `setDragImage` on a hidden off-screen ref element to suppress the browser's default ghost; `mousemove` on `document` updates preview position while `isDragging`; `dragend` clears state; preview rendered via `createPortal` into `document.body` as a `fixed` div using `ShapeRenderer` at the dragged shape's default size.
- **14-node-editing**: `components/editor/canvas-context.tsx` — new React context exporting `CanvasContext` and `useCanvasContext`; provides `onNodesChange: OnNodesChange<CanvasNode>` to node components without prop drilling. `components/editor/canvas-flow.tsx` — wraps `CanvasFlowInner` return with `CanvasContext.Provider` passing the Liveblocks-aware `onNodesChange`. `components/editor/canvas-node.tsx` — added `NodeResizer` (from `@xyflow/react`) shown when `selected`; resize dispatches `dimensions` changes through Liveblocks sync; minimum size clamped to 60×40. Added inline label editing: double-click starts editing, sets local `editValue` from `data.label`; label span becomes `invisible` (preserving layout); absolutely-positioned textarea overlay appears centered; `onBlur` commits via `{ type: "replace" }` change through `onNodesChange`; `Escape` sets a `cancelRef` to skip the save on subsequent blur; `onMouseDown`/`onClick` on the textarea stop propagation to prevent canvas drag/pan.
- **15-node-color-toolbar**: `components/editor/canvas-color-toolbar.tsx` — new client component; renders a `NodeToolbar` (from `@xyflow/react`) anchored above the node via `position={Position.Top}` and `offset={10}`; displays 8 color swatches from `NODE_COLORS`; active swatch shows a 2px ring in its text color; hovering a non-active swatch shows a tight glow (`box-shadow: 0 0 7px 1px <text>60`); all `mouseDown`/`pointerDown`/`click` events stop propagation to prevent node drag and canvas pan; clicking a swatch calls `onNodesChange` with a `replace` change updating `data.color` to the new bg, which updates both background and text color immediately through Liveblocks. `components/editor/canvas-node.tsx` — imports `CanvasColorToolbar`; renders it as `toolbar` variable when `selected`; included in all four shape branches (rectangle, pill, circle, SVG shapes).
- **16-edge-behavior**: `types/canvas.ts` — added `EdgeData` interface (`label?: string`) and updated `CanvasEdge` to use it. `components/editor/canvas-context.tsx` — extended `CanvasContextValue` with `onEdgesChange: OnEdgesChange<CanvasEdge>`; provider in `canvas-flow.tsx` passes it from `useLiveblocksFlow`. `components/editor/canvas-edge.tsx` — new custom edge renderer: `getSmoothStepPath` for right-angle routing with `borderRadius: 8`; wide transparent `<path>` (strokeWidth 20) for easy hit detection; thin visible `<path>` (strokeWidth 1.5) dimmed at rest (`rgba(248,250,252,0.4)`) and bright on hover/selected (`rgba(248,250,252,0.88)`); per-edge `<marker>` arrowhead whose fill updates with stroke color; `EdgeLabelRenderer` positions label at the `getSmoothStepPath` midpoint (`labelX, labelY`); double-click on the edge or label area enters edit mode; auto-growing `<input>` (width approximated from char count); saves on blur, Enter, or Escape; saved labels shown as small pill badges; faint "Add label…" hint shown when active edge has no label; all label interactions use `nodrag nopan` class and stop pointer/mouse event propagation. `components/editor/canvas-flow.tsx` — removed `SmoothStepEdge`; registered `CanvasEdgeComponent` for both `default` and `canvasEdge` edge types; added `defaultEdgeOptions={{ type: "canvasEdge" }}`.
- **18-starter-templates**: `components/editor/starter-templates.ts` — `CanvasTemplate` interface; `n()`/`e()` builder helpers for readability; three templates: Microservices Architecture (API gateway → auth/user/order services → database/notification, using pill, rectangle, cylinder shapes), CI/CD Pipeline (repo → build → test → staging → integration tests → production, using cylinder, rectangle, hexagon), Event-Driven System (producer → event bus → consumer A/B → dead letter queue, using pill, hexagon, rectangle, cylinder). `components/editor/starter-templates-modal.tsx` — `TemplatePreview` pure SVG component: computes bounding box, scales everything to 260×130 viewport with padding, draws edges as `<line>` between node centers, draws nodes as shape-appropriate SVG primitives (circle → `<circle>`, diamond → `<polygon>`, hexagon → 6-point `<polygon>`, pill → fully rounded `<rect>`, rectangle/cylinder → `<rect rx=2>`); `StarterTemplatesModal` dialog: `DialogContent` with `ScrollArea`; 3-column grid of template cards each showing preview + name + description + Import button; import calls `onImport(template)` then closes. `components/editor/canvas-flow.tsx` — adds `isTemplatesOpen`/`onTemplatesOpenChange` props through `CanvasFlowInner` and `CanvasFlow`; `handleImportTemplate` clears existing edges then nodes via `remove` changes, adds template nodes/edges via `add` changes, calls `fitView` with 300ms animation after 50ms; renders `StarterTemplatesModal`. `components/editor/canvas-wrapper.tsx` — forwards `isTemplatesOpen`/`onTemplatesOpenChange` to `CanvasFlow`. `components/editor/workspace-shell.tsx` — adds `isTemplatesOpen` state; passes `onOpenTemplates` to `EditorNavbar`; passes open state to `CanvasWrapper`. `components/editor/editor-navbar.tsx` — adds optional `onOpenTemplates` prop; renders `LayoutTemplate` icon button when provided.
- **19-presence-avatars-cursors**: `liveblocks.config.ts` — renamed `isThinking` → `thinking` in `Presence` type to match spec. `components/editor/canvas-wrapper.tsx` — updated `initialPresence` to use `thinking: false`. `components/editor/presence-avatars.tsx` — new client component; uses `useOthers` from `@liveblocks/react/suspense` and `useUser`/`UserButton` from Clerk; filters collaborators against current Clerk user ID; renders up to 5 overlapping avatar chips (profile photo or initials fallback, colored ring matching participant's presence color, negative-margin overlap stack) with a `+N` overflow chip when needed; divider (border-r) appears only when at least one collaborator is present; `UserButton` always rendered at the right; whole group has semi-transparent surface background + backdrop blur. `components/editor/canvas-flow.tsx` — imports `useUpdateMyPresence` from `@liveblocks/react`; adds `handleMouseMove` (converts screen coords via `screenToFlowPosition`, calls `updateMyPresence({ cursor: position })`) and `handleMouseLeave` (sets cursor to null) wired to `ReactFlow`'s `onMouseMove`/`onMouseLeave`; mounts `PresenceAvatars` in a `Panel position="top-right"` inside the canvas; `Cursors` from `@liveblocks/react-flow` renders live cursors for other participants using their `info.color` and `info.name` from the auth endpoint.
- **20-ai-sidebar-shell**: `components/editor/ai-sidebar.tsx` — new client component; accepts `isOpen`/`onClose` props; preserves existing slide-in animation (`translate-x-full` / `translate-x-0`), floating position, border, `bg-surface/95` background, and shadow; header with `Bot` icon, "AI Workspace" title (`text-copy-primary`), "Collaborate with Ghost AI" subtitle (`text-copy-muted`), and close button; shadcn `Tabs` with "AI Architect" and "Specs" triggers styled via `data-[state=active]:bg-subtle data-[state=active]:text-brand` and inactive `text-copy-muted`; AI Architect tab — scrollable chat area with empty state (bot icon, description, three starter prompt chips styled `bg-subtle text-ai-text`), user messages right-aligned (`bg-accent-dim border-2 border-brand/50 text-copy-primary`), assistant messages left-aligned (`bg-elevated border border-surface-border text-ai-text`), auto-resizing textarea (min 72px / max 160px), send button (`bg-brand text-base`), Enter submits / Shift+Enter newline; Specs tab — Generate Spec button (`bg-brand text-base`), static demo spec card (`bg-elevated border-surface-border`) with `FileText` icon, title, snippet, and disabled download action. `components/editor/workspace-shell.tsx` — replaced inline `<aside>` placeholder with `<AISidebar isOpen={isAISidebarOpen} onClose={...} />`.
- **21-canvas-autosave**: `@vercel/blob` installed. `app/api/projects/[projectId]/canvas/route.ts` — `GET` reads `canvasJsonPath` from Prisma, fetches JSON from Vercel Blob, and returns it; `PUT` receives canvas JSON, uploads to Vercel Blob at `canvas/{projectId}.json` with `allowOverwrite: true`, stores returned URL in Prisma `canvasJsonPath`; both routes enforce auth and project access via `getCurrentIdentity`/`canAccessProject`. `hooks/use-canvas-autosave.ts` — watches `nodes`/`edges` props; skips first render; debounces saves by 1500ms; PUTs to canvas API; tracks `SaveStatus` (`idle` | `saving` | `saved` | `error`). `components/editor/canvas-flow.tsx` — imports autosave hook and load-on-mount logic: on mount, if Liveblocks room is empty, fetches saved canvas from GET route and applies nodes/edges via `onNodesChange`/`onEdgesChange` then `fitView`; if room already has data, skips load; propagates `saveStatus` to parent via `onSaveStatusChange` callback. `components/editor/canvas-wrapper.tsx` and `workspace-shell.tsx` — extended with `onSaveStatusChange` prop chain; `workspace-shell.tsx` holds `saveStatus` state and passes it to `EditorNavbar`. `components/editor/editor-navbar.tsx` — added `saveStatus` prop; center section shows animated spinner + "Saving", checkmark + "Saved", or X + "Error saving" beside the project name.
- **17-canvas-ergonomics**: `components/editor/canvas-control-bar.tsx` — new client component; pill-shaped floating bar at bottom-left of canvas; zoom out / fit view / zoom in buttons wired to `useReactFlow()` with 200ms animation duration; thin divider separates zoom and history groups; undo/redo buttons wired to `useUndo`/`useRedo` from `@liveblocks/react`; disabled state driven by `useCanUndo`/`useCanRedo`; disabled buttons render at `opacity-30`. `hooks/useKeyboardShortcuts.ts` — new hook; receives React Flow instance and undo/redo handlers as parameters; attaches a `keydown` listener to `window`; skips editable targets (`input`, `textarea`, `contentEditable`); handles `+`/`=` (zoom in), `-` (zoom out), `Cmd/Ctrl+Z` (undo), `Cmd/Ctrl+Shift+Z` (redo), `Cmd/Ctrl+Y` (redo). `components/editor/canvas-flow.tsx` — imports `useUndo`/`useRedo` from `@liveblocks/react`; calls `useKeyboardShortcuts`; mounts `CanvasControlBar` in a `Panel position="bottom-left"` above the shape panel.

## In Progress

- None.

## Bug Fixes

- **unique-project-name**: Added `@@unique([ownerId, name])` to `Project` model so duplicate project names per owner are rejected at the database level. Migration `20260524000000_add_project_name_unique_per_owner` deduplicates existing rows (keeping most recent) then creates the unique index. `POST /api/projects` and `PATCH /api/projects/[projectId]` both return 409 with `"a project with that name already exists"` on P2002 name conflicts.

## Next Up

- Add the next planned feature unit here.


## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Prisma 7 uses `prisma.config.ts` (not schema-embedded `url`) for datasource config; `prisma/` directory is the multi-file schema root.
- Prisma 7 generates client to `app/generated/prisma/`; import as `@/app/generated/prisma/client`, not `@prisma/client`.
- `PrismaClientOptions` in Prisma 7: `{ adapter }` for direct PG, `{ accelerateUrl }` for Accelerate — mutually exclusive union.
- shadcn/ui on Tailwind v4 — CSS variables defined in `globals.css` via `@theme inline`; no `tailwind.config.js` needed.
- Dark-only theme: project custom properties (`--bg-base`, `--text-primary`, etc.) defined once in `:root` with no `.dark` override. shadcn semantic variables (`--background`, `--foreground`, etc.) are mapped to project variables.
- Tailwind utilities for project tokens: `bg-base`, `bg-surface`, `bg-elevated`, `bg-subtle`, `text-copy-primary`, `text-copy-muted`, `border-surface-border`, `text-brand`, `bg-accent-dim`, `text-ai`, `text-ai-text`, `text-error`, `text-success`, `text-warning`.
- Auth uses `proxy.ts` (Next.js 16's renamed middleware convention) — not `middleware.ts`. Export name is `proxy`, not `middleware`.
- Clerk appearance: `dark` theme from `@clerk/ui/themes` passed as `theme` key in `appearance`; CSS variables used for all color overrides via `variables` key.

## Session Notes

- Project uses Next.js 16.2.6, React 19, Tailwind v4 (`@import "tailwindcss"`), TypeScript strict mode.
- Path alias `@/*` maps to project root.
- `components/ui/*` files are generated by shadcn — do not modify them directly.
- shadcn component library: `base-nova` style (uses `@base-ui/react` primitives + Lucide icons).
- Next.js 16 renames `middleware.ts` to `proxy.ts` and the exported function from `middleware` to `proxy`.
- `@clerk/ui` installed; provides `dark`, `neobrutalism`, `shadcn`, `shadesOfPurple` themes.
